# 语义通信模块状态

项目方案计划使用 TLV + CRC + ZeroMQ 实现船岸弱网语义传输，但已审计材料中没有发现可发布实现。v0.1.0 仅保留接口目标，不声称链路已经跑通。

建议最小实现：

1. JSON token 到版本化 TLV 的序列化/反序列化。
2. CRC 校验与错误注入测试。
3. ZeroMQ PUSH/PULL 发送端和接收端。
4. HWM、本地 SQLite/文件队列、断线重连和复网重发。
5. `event_id` 幂等去重。
6. TC/NetEm 弱网测试及自动化指标记录。

