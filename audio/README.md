# SenseVoice 音频模块状态

SenseVoice 是项目设计的一部分，但在已审计的本地项目材料中，没有找到可发布的 SenseVoice 推理脚本、模型配置、锁定依赖、测试音频清单或性能报告。因此 v0.1.0 不包含伪造的音频实现。

后续实现至少应固定：

- 上游仓库和模型精确版本/commit。
- 模型 ID、模型许可证和权重下载方式。
- 输入采样率、声道、窗口长度和重采样方法。
- 输出字段：转写、语言、关键词/呼救事件、情绪或声学事件、置信度、时延。
- Jetson 上的运行方式和资源消耗。
- 与视觉 token 一致的事件 schema。

